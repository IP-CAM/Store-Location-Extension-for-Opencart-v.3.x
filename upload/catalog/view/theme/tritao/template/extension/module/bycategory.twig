<div class="header-border"><h3 class="heading"><a href="{{ category_link }}">{{ heading_title }} </a></h3>
</div>
<div class="row">
    {% for product in products %}
        <div class="product-layout col-lg-3 col-md-3 col-sm-6 col-xs-12 wow fadeInUp slider animated" data-wow-duration="1s" data-wow-delay="100ms" style="visibility: visible; animation-duration: 1s; animation-delay: 100ms; animation-name: fadeInUp;">
            <div class="product-thumb transition">
                <div class="image"><a href="{{ product.href }}" ><img src="{{ product.thumb }}" alt="{{ product.name }}" title="{{ product.name }}" class="img-fluid" /></a></div>
                <div class="caption">
                    <h4><a href="{{ product.href }}">{{ product.name }}</a></h4>
                    <div class="description">{{ product.description }}</div>
                   {% if product.rating %}
            <div class="rating">
              {% for i in 1..5 %}
                {% if product.rating < i %}
                  <span class="fas fa-stack"><i class="fas fa-star-o fa-stack-2x"></i></span>
                {% else %}
                  <span class="fas fa-stack"><i class="fas fa-star fa-stack-2x"></i><i class="fas fa-star-o fa-stack-2x"></i></span>
                {% endif %}
              {% endfor %}
            </div>
          {% endif %}

                    {% if product.price %}
                    <p class="price">
                            {% if not product.special %}
                                {{ product.price }}
                            {% else %}
                                <span class="price-new">{{ product.special }}</span> <span class="price-old">{{ product.price }}</span>
                            {% endif %}
                            {% if product.tax %}
                                <span class="price-tax">{{ text_tax }} {{ product.tax }}</span>
                            {% endif %}
                        </p>
					 {% endif %}
                </div>

                <div class="buttons-wrapper">
                    <div class="button-group">
                        <div class="cart-button">
                            {% if  product.quantity < 1  %}
                            <span class="out-of-stock"> Produto sem estoque</span>
                            {% elseif product.price %}
                            {% if vendedor < 30 %}
                            <a class="btn btn-primary" onclick="cart.add('{{ product.product_id }}');"><i class="fa fa-shopping-cart"></i> <span class="hidden-xs hidden-sm hidden-md">{{ button_cart }}</span></a>

                            {% endif %}
                            {% if vendedor or customer_group_id != 2 or customer_group_id == 3 %}
                            <span class="btn btn-primary btn-lg btn-block" style="background-image:none; background-color: #4caf50; border-color: #33b538;width: 242px;"><i class="fa fa-shopping-cart"></i> Disponível</span>
                            {% endif %}
                             {% else %}
                            {% if product.quantity >= 1 %}
                            <a href="/acessar" class="login-price"> Faça login para visualizar os preços</a>
                            {% else %}
                            <span class="out-of-stock"> Produto sem estoque</span>
                            {% endif %}
                            {% endif %}
                        </div>
                      </div>
                </div>
            </div>
        </div>
    {% endfor %}
</div>
